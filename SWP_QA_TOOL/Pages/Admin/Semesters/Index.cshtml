@page
@model PresentationLayer.Areas.Admin.Pages.Semesters.IndexModel

@{
    ViewData["Title"] = "Semesters";
}

<h2>Semesters</h2>

<p>
    <a asp-page="Create" class="btn btn-primary">Create New</a>
</p>

@if (TempData["Error"] != null)
{
    <div class="alert alert-danger">@TempData["Error"]</div>
}

<table class="table table-bordered table-striped">
    <thead>
        <tr>
            <th>ID</th>
            <th>Code</th>
            <th>Name</th>
            <th>StartDate</th>
            <th>EndDate</th>
            <th>IsActive</th>
            <th>IsCurrent</th>
            <th style="width:220px;">Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Semesters)
        {
            <tr>
                <td>@item.SemesterId</td>
                <td>@item.SemesterCode</td>
                <td>@item.SemesterName</td>
                <td>@item.StartDate.ToString("dd/MM/yyyy")</td>
                <td>@item.EndDate.ToString("dd/MM/yyyy")</td>
                <td>@(item.IsActive == true ? "Yes" : "No")</td>
                <td>@(item.IsCurrent == true ? "Yes" : "No")</td>
                <td>
                    <a asp-page="Details" asp-route-id="@item.SemesterId" class="btn btn-sm btn-info">Details</a>
                    <a asp-page="Edit" asp-route-id="@item.SemesterId" class="btn btn-sm btn-warning">Edit</a>
                    <a asp-page="Delete" asp-route-id="@item.SemesterId" class="btn btn-sm btn-danger">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
